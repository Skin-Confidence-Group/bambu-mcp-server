# Bambu Lab Cloud API Credentials
BAMBU_EMAIL=your-email@example.com
BAMBU_PASSWORD=your-password
BAMBU_TOKEN=
BAMBU_DEVICE_ID=0948BB5B1200532

# Railway Port (auto-assigned in production)
PORT=8000

# MCP Server Settings
MCP_SERVER_NAME=bambu-printer
MCP_SERVER_VERSION=0.1.0

# Setup Security (optional - protects /setup endpoints)
# Generate with: openssl rand -hex 32
SETUP_KEY=
